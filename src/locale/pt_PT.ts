/*
 * This file is automatically generated.
 * Run 'pnpm run generate:locales' to update.
 */

import { Faker } from '../faker';
import en from '../locales/en';
import pt_PT from '../locales/pt_PT';

const faker = new Faker({
  localeOrder: ['pt_PT', 'en'],
  locales: {
    pt_PT,
    en,
  },
});

export = faker;
