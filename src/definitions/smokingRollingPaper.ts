import type { LocaleEntry } from './definitions';

/**
 * The possible definitions related to smoking rolling paper.
 */
export type SmokingRollingPaperDefinitions = LocaleEntry<{
  brand: string[];
  size: string[];
}>;
