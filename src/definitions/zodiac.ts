/**
 * The possible definitions related to zodiac.
 */
export interface ZodiacDefinitions {
  /**
   * Generate a random zodiac sign or a sign based on the birthdate.
   */
  sign: {
    aquarius: string;
    pisces: string;
    aries: string;
    taurus: string;
    gemini: string;
    cancer: string;
    leo: string;
    virgo: string;
    libra: string;
    scorpio: string;
    sagittarius: string;
    capricorn: string;
  };
}
