import type { LocaleEntry } from './definitions';

/**
 * The possible definitions related to smoking filter.
 */
export type SmokingFilterDefinitions = LocaleEntry<{
  brand: string[];
  size: string[];
}>;
