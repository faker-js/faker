import type { LocaleDefinition } from '../..';

const id: LocaleDefinition = {
  title: 'Indonesia',
  address: require('./address'),
  company: require('./company'),
  internet: require('./internet'),
  date: require('./date'),
  name: require('./name'),
  phone_number: require('./phone_number'),
};

export default id;
