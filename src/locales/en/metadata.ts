import type { MetadataDefinitions } from '../..';

const metadata: MetadataDefinitions = {
  title: 'English',
  code: 'en',
  language: 'en',
  endonym: 'English',
  rtl: false,
  script: 'Latn',
};

export default metadata;
