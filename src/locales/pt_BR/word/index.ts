import type { WordDefinition } from '../../../definitions';
import adjective from './adjective';
import noun from './noun';

const word: WordDefinition = {
  noun,
  adjective,
};

export default word;
