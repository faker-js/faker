import type { MetadataDefinitions } from '../..';

const metadata: MetadataDefinitions = {
  title: 'Japanese',
  code: 'ja',
  language: 'ja',
  endonym: '日本語',
  rtl: false,
  script: 'Jpan',
};

export default metadata;
